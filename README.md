# NLA TUI

Real-time visualization of AI "thoughts".

## Install
```bash
pip install -e .
```

## Verify
```bash
python verify_interceptor.py
```

## Run
1. Start SGLang server for the NLA model.
2. Run `nla-cli chat --model <model_id> --sglang-url <url>`.
